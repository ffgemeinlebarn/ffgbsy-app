import { EigenschaftenChangedToMitPipe } from './eigenschaften-changed-to-mit.pipe';

describe('EigenschaftenChangedToMitPipe', () => {
  it('create an instance', () => {
    const pipe = new EigenschaftenChangedToMitPipe();
    expect(pipe).toBeTruthy();
  });
});

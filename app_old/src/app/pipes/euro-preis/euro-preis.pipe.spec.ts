import { EuroPreisPipe } from './euro-preis.pipe';

describe('EuroPreisPipe', () => {
  it('create an instance', () => {
    const pipe = new EuroPreisPipe();
    expect(pipe).toBeTruthy();
  });
});
